schema: ./src/schema/typedefs.graphql
overwrite: true
generates:
  ./types/graphql.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      mappers:
        Message: ../db#IMessage
        Chat: ../db#IChat
        User: ../db#IUser
      scalars:
        Date: Date
        URL: String
