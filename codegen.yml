schema: ./src/schema/typedefs.graphql
overwrite: true
generates:
  ./types/graphql.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      scalars:
        Date: Date
        URL: String
